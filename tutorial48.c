#include <stdio.h>
#include <stdlib.h>

int main()
{
    return 0;
}
// Dynamic memory allocation

/*
    ABC Pvt ltd. manages employee record of other companies.
    Employee id can be of any length and it can contain any character.
    You have to take length of employee id as an input in integer.
    for 3 employes you have to take employee id  as input and display it on screen.
    Store the employee id in a character array which is allocated dynamically.
    You have to create one character array dynamically.

    Employee 1:
    Enter no of characters in your eid:
    40
    dynamically allocate the character id.
    take input from the user


    Employee 2:
    Enter no of characters in your eid:
    40
    dynamically allocate the character id.
    take input from the user

    Employee 3:
    Enter no of characters in your eid:
    40
    dynamically allocate the character id.
    take input from the user

*/
